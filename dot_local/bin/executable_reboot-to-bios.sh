#!/usr/bin/env bash

# ~/.local/bin/reboot-to-bios.sh
# =============================================================================
# A script to reboot into the BIOS

systemctl reboot --firmware-setup
